---
type Props = { className?: string; href: string; label: string };

const { className, href, label } = Astro.props;
---

<a class={`${className} capitalize hover:underline`} href={href}>{label}</a>
