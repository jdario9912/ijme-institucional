/// <reference types="astro/client" />

type Ministerio = {
  imagePath: string;
  icono: string;
  titulo: string;
  descripcion: string;
};

type Versiculo = {
  texto: string;
  cita: string;
};

type Faq = {
  pregunta: string;
  respuesta: string;
};
