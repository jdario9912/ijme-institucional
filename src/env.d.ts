/// <reference types="astro/client" />

type Ministerio = {
  icono: string;
  titulo: string;
  descripcion: string;
};

type Versiculo = {
  texto: string;
  cita: string;
};

type Faq = {
  pregunta: string;
  respuesta: string;
};

type Navegation = {
  href: string;
  label: string;
};

type SocialNavegation = {
  href: string;
  icono: string;
};
